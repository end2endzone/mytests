dist: trusty
sudo: false
language: cpp
os:
- linux
branches:
  only:
  - master
env:
  global:
  - PROJECT_NAME=test

before_install:
- echo Java version is...;
  java -version;
- echo Apache Ant version is...;
  ant -version;
- env
script:
- echo THIS IS SCRIPT!
after_success:
- echo THIS IS AFTER SUCCESS!

notifications:
  email: true
